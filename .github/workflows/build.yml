name: Build OnePlus 5T 4.4 Kernel with KernelSU

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Install Build Dependencies
        run: |
          sudo apt update
          sudo apt install -y git build-essential bc bison flex libssl-dev libelf-dev clang lld gcc-aarch64-linux-gnu

      - name: Clone LineageOS 22.2 Kernel
        run: |
          git clone https://github.com/LineageOS/android_kernel_oneplus_msm8998.git -b lineage-22.2 --depth=1 kernel
          cd kernel

      - name: Integrate KernelSU
        run: |
          cd kernel
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -s v0.9.5
          echo "CONFIG_KPROBES=y" >> arch/arm64/configs/lineageos_dumpling_defconfig
          echo "CONFIG_HAVE_KPROBES=y" >> arch/arm64/configs/lineageos_dumpling_defconfig
          echo "CONFIG_KPROBE_EVENTS=y" >> arch/arm64/configs/lineageos_dumpling_defconfig

      - name: Build Kernel
        run: |
          cd kernel
          export ARCH=arm64
          export SUBARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          make lineageos_dumpling_defconfig
          make -j$(nproc) CC=clang

      - name: Package with AnyKernel3
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git
          cp kernel/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          cd AnyKernel3
          sed -i 's/device.name1=.*/device.name1=dumpling/' anykernel.sh
          sed -i 's/device.name2=.*/device.name2=cheeseburger/' anykernel.sh
          zip -r9 ../Lineage22_dumpling_KernelSU.zip * -x .git README.md *placeholder

      - name: Upload Flashable ZIP
        uses: actions/upload-artifact@v4
        with:
          name: KernelSU-Lineage22-dumpling
          path: Lineage22_dumpling_KernelSU.zip
